# Junior-Invest — הריפו עובר ייעוד

**הריפו הזה ריק מאפליקציה כרגע, וממתין לאפליקציה חדשה.**

תיק ההשקעות שהיה כאן פעם חי בריפו `lironcon2015-prog/homebudget` תחת `invest/`,
ומוגש מ-<https://homebudget.lironcon.com/invest/>. הוא לא חוזר לכאן.

## מה קרה לדף ההעברה

עד לאחרונה `index.html` כאן היה דף העברת נתונים: הוא קרא מפתחות
`juniorinvest:*` מ-`localStorage`, דחס אותם והעביר אותם ב-fragment של ה-URL
לכתובת החדשה. **הדף נמחק בכוונה** אחרי שבעל הריפו אישר שאין יותר דפדפן שמחזיק
נתונים ישנים בכתובת הזו.

המשמעות: נתונים תחת origin-ים ישנים (`invest.lironcon.com`) כבר אינם נגישים —
`localStorage` הוא לפי origin, ואין יותר גשר. זו החלטה מודעת ולא ניתנת לשחזור
בלי להחזיר את הדף. הקוד עצמו שמור בהיסטוריה: `git show 15fb22a:index.html`.

צד הקליטה עדיין חי ב-bootstrap של `invest/index.html` בריפו `homebudget` (קורא
`#import=` מה-hash וממזג מפתחות `juniorinvest:*`). הוא הפך לקוד מת — אפשר
לנקות אותו שם בהזדמנות, אבל הוא לא מזיק.

## מה מותר לשנות כאן

הכל. האיסור הישן על קוד אפליקציה בריפו הזה בוטל — הוא נועד למנוע *שני עותקים
חיים של אותה אפליקציה* על שני origins, וזה כבר לא המצב. אפליקציה **חדשה ושונה**
כאן היא בדיוק מה שמתוכנן.

עדיין בתוקף: אל תחזיר לכאן את קוד תיק ההשקעות. לפיתוח שלו —
`invest/CLAUDE.md` בריפו `homebudget`.

## מצב נוכחי

```
CNAME     invest.lironcon.com
icons/    8 קבצים מהמיתוג הישן (Kids Portfolio) — סגול/ירוק, ממתינים להחלפה
```

אין `index.html`, אין CI, אין תלויות. עד שהאפליקציה החדשה תיכנס,
`invest.lironcon.com` מחזיר 404.

## פריסה

GitHub Pages מ-`main` בשורש הריפו, בלי workflow. `CNAME` מצמיד את
`invest.lironcon.com`. push ל-`main` הוא הפריסה — אין שלב build כרגע, וזו
החלטה פתוחה שתיסגר עם מפרט האפליקציה החדשה.

## בדיקה מקומית

`python3 -m http.server` ואז פתיחה בדפדפן.
