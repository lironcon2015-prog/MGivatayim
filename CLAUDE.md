# מכבי גבעתיים — העונה

אפליקציית ווב שמרכזת עונת כדורגל אחת של קבוצת נוער: המשחק הבא, תוצאות, נתוני
שחקנים, סרטונים וקישורים. קהל היעד הוא השחקנים וההורים, לא המאמן — המטרה
היא להחליף חיפוש בוואטסאפ, לא לנהל את הקבוצה.

עברית, RTL, mobile-first, כהה בלבד. מודולי ES טבעיים, בלי תלויות ובלי build.

## שפה: כל התקשורת עם בעל הריפו בעברית

תשובות, סיכומים, שאלות, הסברים והוראות התקנה — **בעברית, תמיד**, גם אחרי
עבודה ארוכה בקוד ובפלט באנגלית. זו בקשה מפורשת של בעל הריפו, אחרי שהשיחה
גלשה לאנגלית יותר מפעם אחת. הערות בקוד והודעות commit נשארות באנגלית.

## עבודה עם git — תמיד ל-`main`

**כל שינוי נדחף וממוזג ל-`main` בסוף העבודה**, בלי לחכות לאישור נוסף. זו
הוראה קבועה של בעל הריפו. אם הסשן עובד על ענף אחר, ממזגים אותו ל-`main`
(fast-forward או merge commit, לא rebase על היסטוריה שכבר נדחפה) ודוחפים
את `main`. אין PR כברירת מחדל — רק אם התבקש במפורש.

הסיבה: `main` הוא הפריסה. שינוי שנשאר על ענף לא קיים מבחינת המשתמשים.

**לפני הדחיפה ל-`main` ארבע הסוויטות עוברות** (ראו "בדיקות"), ו**הגרסה
מקודמת** (ראו "גרסאות ועדכונים"). אחרי הדחיפה
חוזרים לענף העבודה ומיישרים אותו ל-`main` — ענף שנשאר מאחור הוא איך
שינוי נבנה על בסיס ישן.

**בתחילת סשן:** `git fetch origin main` ויישור ענף העבודה אליו. לפעמים
סוכן נוסף עובד על הריפו במקביל, ו-`main` זז בינתיים.

## מפת הקבצים

| קובץ | תפקיד |
|---|---|
| `index.html`, `styles.css`, `manifest.webmanifest`, `sw.js`, `version.json` | המעטפת, העיצוב כולו, PWA |
| `src/app.js` | ניתוב (`#/`, `#/live`, `#/stats`, `#/media`, `#/admin`), מצב גישה, ניווט |
| `src/config.js` | כתובת הגשר (`BRIDGE_URL`) וסמל ברירת מחדל |
| `src/bridge.js` | `call(action, params, {asAdmin})` — הקריאה היחידה לגשר |
| `src/store.js` | localStorage בקידומת `mg:` — `device`, `name`, `admin` (קוד המנהל), `season` (עותק במטמון, כולל `role` ו-`coach`), `alerted` (התראת הדקות האחרונה), `gallery` (התשובה האחרונה של `getGallery`), `liveLast` (המשחק החי האחרון שאושר), `folded` (התראה שהמאמן קיפל ב"הבנתי"), `bridge` (דריסה לבדיקות), `pollMs` |
| `src/season.js` | `buildSeason` — כל מה שמחושב |
| `src/format.js`, `src/components.js`, `src/icons.js`, `src/ui/sheet.js` | עיצוב נתונים (`esc`, תאריכים, שמות), רכיבי ממשק, אייקונים, גיליון תחתון |
| `src/views/*.js` | מסך לכל נתיב; `gate.js` = בקשת גישה והמתנה |
| `src/live/model.js`, `src/live/sync.js` | משחק חי (ראו למטה) |
| `src/minutes.js`, `src/views/minutes.js` | דקות משחק למאמן: חישוב טהור, ומסכים (ראו "המאמן") |
| `src/fixtures.js`, `src/importer.js`, `src/positions.js`, `src/posters.js`, `src/imaging.js`, `src/install.js`, `src/updater.js` | ראו הסעיפים המתאימים |
| `src/gallery.js`, `src/views/gallery.js` | הגלריה של הקבוצה: העלאה ל-Cloudinary, והמסך (ראו "הגלריה") |
| `tools/bridge.gs` | הגשר (Apps Script) |
| `tools/bump.mjs` | קידום גרסה |
| `tools/make-fixtures.py` | מייצר את `docs/fixtures/*.xlsx` (`openpyxl`) — לא לערוך את הקבצים ידנית |
| `docs/coach.html` | "מדריך למאמן" — דף עצמאי באתר (הגרסה בתחתית נקראת מ-`version.json`), עם תגיות og ותמונת תצוגה `docs/og-coach.jpg` (1200×630) לוואטסאפ (`…/MGivatayim/docs/coach.html`), לשליחה למאמן. שינוי במסכי המאמן → עדכון שלו |
| `docs/parent.html` | "מדריך להורים" — אותו מבנה, בלי דקות, ועם תיעוד משחק בקוד שליטה כשהמנהל לא במגרש. שינוי במסך הלייב או בתיעוד → עדכון שלו |
| `README.md` | הוראות למנהל: התקנת הגשר בפעם הראשונה, `ADMIN_CODE` (12 תווים לפחות), פריסה |

כניסת מנהל: `#/admin` → קוד המנהל, שנשמר ב-`mg:admin` במכשיר. אין משתמשים
ואין סיסמאות מעבר לזה.

## ארכיטקטורה: הגשר

הנתונים **לא** נמצאים בריפו. הם יושבים בתיקייה `MGivatayim` בגוגל דרייב של
המנהל, והאפליקציה מדברת איתם רק דרך `tools/bridge.gs` — Apps Script שפרוס
כ-Web app ("Execute as: Me", "Who has access: Anyone"). הדפוס מ-`family-vault`.

**כתובת הגשר ציבורית** (`src/config.js`, בריפו ציבורי). לכן היא לא מגינה על
כלום, והגשר אוכף הכל בעצמו:

| מי | מזוהה ב- | רשאי |
|---|---|---|
| מכשיר לא מוכר | מפתח מכשיר אקראי | לבקש גישה, לשאול מה מצב הבקשה |
| מכשיר מאושר | מפתח מכשיר שהמנהל אישר | לקרוא את נתוני העונה ולצפות במשחק החי |
| מכשיר שולט | מכשיר מאושר שמימש קוד חד-פעמי | לעדכן את המשחק החי הנוכחי בלבד, עד סיומו |
| מאמן | מכשיר מאושר שהמנהל סימן (`role: 'coach'` ב-`access.json`, `setRole`) | לקרוא גם דקות והרכבי עבר, ולכתוב רק את נתוני המאמן (`setCoachMatch`) |
| מנהל | קוד מנהל (`ADMIN_CODE` ב-Script properties) | הכל: לקרוא, לכתוב, לאשר, לבטל |

כללים שאסור לשבור:

- **הרשאה נבדקת בגשר, לא בלקוח.** הסתרת כפתור בממשק היא נוחות, לא הגנה.
- **הגשר שומר גיבוב של מפתח המכשיר, לא את המפתח.** `tests/bridge.mjs` בודק
  שהמפתח הגולמי לא מגיע לדרייב.
- **קוד המנהל לא נכתב בקוד.** הוא ב-Script properties של הפרויקט ב-Apps Script,
  כי הקובץ `bridge.gs` נמצא בריפו ציבורי.
- **`fetch` לגשר בלי כותרות.** גוף מחרוזת בלי `Content-Type` נשלח כ-`text/plain`
  ולכן אין preflight. Apps Script לא עונה ל-OPTIONS; הוספת
  `Content-Type: application/json` שוברת כל קריאה. הגשר המדומה בבדיקות מסרב
  ל-OPTIONS כדי שזה ייתפס.
- **שמירה נדחית אם הבסיס התיישן** (`baseVersion`), במקום לדרוס בשקט.
- **דקות משחק לשחקן — למנהל ולמאמן בלבד.** `forParents_` בגשר מסיר ממה
  שהורה מקבל את `players[].minutes` ואת `matches[].lineup` של משחקים
  שהסתיימו: מההרכב הפותח והחילופים הדקות מחושבות. האירועים נשארים (ציר זמן,
  כובשים). `getSeason` מחזיר `role` (`admin`/`coach`/`parent`), ורק למנהל
  ולמאמן את העונה המלאה ואת `coach`. בממשק מסכי הדקות מוסתרים
  (`s.showMinutes`) — נוחות, לא ההגנה. שום תצוגה של הורה לא נשענת על הרכב
  של משחק עבר; מי שמוסיף כזו פותח מחדש את חישוב הדקות. במשחק חי ההורים
  רואים הרכב וחילופים בזמן אמת — זה בכוונה, ואינו נסגר.
- **מאמן לא כותב כלום חוץ מנתוני המאמן** — לא עונה, לא לייב (בלי קוד שליטה),
  לא גישה. `tests/bridge.mjs` בודק כל אחד.
- **מצב משחק חי מגיע ממכשיר לא אמין.** מכשיר שולט כותב את כל ה-state, ולא
  חייב לעשות זאת דרך האפליקציה. התצוגות מדפיסות מספרים ומזהים ל-HTML בלי
  `esc`, ולכן כל state שמגיע מהגשר עובר `cleanLive` (ב-`sync.js`) וכל משחק
  בעונה עובר `cleanPlayedMatch` (ב-`buildSeason`) — בנייה מחדש שדה-שדה עם
  טיפוס כפוי. בלי זה "מספר חולצה" של HTML רץ אצל כל הצופים, כולל המנהל, וקורא
  את קוד המנהל מ-localStorage. `tests/e2e.mjs` מזריק כזה ובודק. שדה חדש
  ב-state → שורה ב-`cleanLive`, אחרת הוא נזרק. בגשר: אחרי הסיום רק המנהל
  מתקן את המשחק (`finishLive` מחזיר `conflict` להורה).
- **אין דריסה של כתובת הגשר מה-URL.** בדיקות מצביעות לגשר מקומי דרך
  `localStorage['mg:bridge']` בלבד. פרמטר ב-URL היה מאפשר לשלוח למנהל קישור
  שמפנה את האפליקציה לגשר זר ואוסף את הקוד שלו.

אחרי כל שינוי ב-`bridge.gs` המנהל צריך לפרוס גרסה חדשה (Deploy → Manage
deployments → New version). בלי זה הכתובת ממשיכה להריץ את הקוד הישן — לציין
את זה בכל פעם שהקובץ משתנה.

## משחק חי

`src/live/model.js` — reducer טהור: כל פעולה נושאת את הזמנים שלה (`at`, `atMs`),
בלי קריאת שעון בפנים. זה מה שמאפשר לשלוט בלי לחכות לרשת: `src/live/sync.js`
מחיל פעולה בטלפון מיד, שומר אותה בתור (ב-localStorage, שורד רענון), ושולח
ברקע. התנגשות → מביאים את גרסת השרת ומריצים את התור מחדש עליה. פעולה
שהפכה חסרת משמעות (שריקת פתיחה כפולה משני טלפונים) מוחזרת ללא שינוי ולא
זורקת — אחרת היא הייתה תוקעת את התור.

- **משחק שנפתח מוקדם מוסתר מההורים** (בקשת בעל הריפו: הרכב מוכן הרבה לפני
  המשחק). `startLive` במצב `setup` שם `meta.hidden` (של הגשר בלבד, לקוח לא
  כותב אותו); `seesLive_` נותן את המשחק רק למנהל, למאמן ולמכשיר שולט, והורה
  מקבל `state: null`. "פרסום להורים" (`publishLive`, מנהל בלבד) מעלה גרסה כדי
  שהורה ששואל `since` יקבל אותו; הסתרה חוזרת רק לפני שריקת הפתיחה. שריקת
  הפתיחה (`putLive` עם status שאינו `setup`) מפרסמת לבד — משחק לא מתועד בלי
  שאף הורה רואה. `hidden: true` בתשובה למי שרואה משחק מוסתר (`hiddenNote`).
- **המצב האחרון שהשרת אישר נשמר בטלפון** (`mg:liveLast`, עד 6 שעות, לא משחק
  שהסתיים): טלפון שנפתח מחדש במגרש בלי קליטה (אייפון סוגר אפליקציה ברקע
  בקלות) מציג את המשחק וממשיך לתעד לתור, במקום "מתחבר למשחק…" עד שהקליטה
  חוזרת. צופה בלי חיבור מקבל שורת "אין חיבור". `forgetAccess` מוחק אותו.
- **צופים שואלים, לא מקבלים דחיפה.** `getLive` עם `since`: תשובת "אין חדש"
  בלי גוף. כל 4 שניות במסך החי, כל 20 מחוצה לו, ומיד בחזרה לחזית.
- **ציור מחדש רק כשמשהו השתנה, ותמונות לא נטענות מחדש** (בעל הריפו: "תמונות
  קופצות"). `fetch` ב-`sync.js` מודיע רק על שינוי; `control` מגיע רק למנהל,
  והשוואה של `undefined` מול `null` השמור נקראה פעם כשינוי בכל שאילתה — מסך
  הלייב של כל הורה צויר מחדש כל 4 שניות. וכשכן מציירים מחדש (שער, חילוף, מונה
  צופים), `keepImages` (ב-`components.js`) מחזיר למקומה כל תמונה שכבר נטענה
  (אותו `src`) במקום אלמנט חדש שנטען ומהבהב — ב-`render` של `app.js` ושל הלייב,
  לפני ה-wire. תצוגה חדשה שמתרעננת מעצמה — אותו דבר. e2e בודק את שניהם.
- **השעון לא נשאל מהשרת.** הוא מחושב מקומית מ-`startedAt` ומהיסט שעון השרת,
  ולכן רץ חלק וזהה בכל הטלפונים. תוספת זמן = השעון ממשיך אחרי אורך החלק.
- **דקות מוצגות מבודדות כיוונית** (`M.ltr`, תווי LRI/PDI). בעברית הגרש של
  `23'` והפלוס של `+01:23` קופצים לצד הלא נכון. הבידוד נעשה בנתון עצמו, כדי
  שיעבוד בכל מקום שהוא נוחת — כפתור, טוסט, כותרת.
- **כפתור בוחר (picker) מקבל handler אחד** — `el.onclick =`, לא
  `addEventListener`. בוחר שמתרנדר מחדש לשלב הבא (מבקיע → מבשל) צבר מאזין
  שני, ולחיצה אחת רשמה שער פעמיים. הבדיקה ב-e2e סופרת אירועי שער.
- **הסיום נכתב לעונה בגשר** (`finishLive`), כי להורה ששולט אין הרשאת כתיבה
  לעונה. התוצאה נספרת מהאירועים בשרת, והשורה מזוהה ב-`liveId` — סיום חוזר
  מחליף ולא משכפל. רענון העונה בלקוח מופעל על מה ש**השרת** אישר, לא על
  התצוגה האופטימית, אחרת הטלפון שלחץ "סיום" קורא את העונה לפני שהתוצאה הגיעה.
- **צופים בלייב — למנהל בלבד** (בקשת בעל הריפו): מסך הלייב שולח `watching`
  ב-`getLive` (`S.watching`, רק כשהמסך פתוח), והגשר שם חותמת זמן במטמון
  (`watch:<hash>`, מפתח לכל מכשיר, **לא בדרייב** — כתיבה כל 4 שניות מכל טלפון
  הייתה חונקת אותו). המנהל מקבל ב-`control.watchers` את המכשירים המאושרים
  שנראו ב-30 השניות האחרונות (`WATCH_FRESH_MS`); בממשק: "👁 N" בראש לוח
  התוצאות וגיליון עם השמות. המנהל עצמו לא נספר. לא מוזכר במדריך להורים — בעל הריפו ביקש להוריד.
- **קוד שליטה להורה:** נשמר מגובב, חד-פעמי, ננעל אחרי 5 ניסיונות שגויים.
  `meta` (מי שולט, הקוד) של הגשר בלבד — לקוח לא יכול לכתוב אותו דרך ה-state.

- **גודל הרכב** (`size`: 9 או 11, `M.SIZES`): ברירת מחדל לעונה ב-`settings.size`,
  ניתן לשינוי למשחק מסוים לפני שריקת הפתיחה. הרכב מלא לא מקבל שחקן נוסף.
  משחק בלי `size` נקרא כ-9.
- **ההרכב הפותח של המשחק האחרון** שתועד (`M.previousLineup`) הוא ברירת המחדל
  למשחק חדש — בלי שחקנים שעזבו, וחתוך לגודל.
- **כל משחק מהלוח יכול לעלות לייב עכשיו** (מנהל בלבד, "משחק אחר מהלוח…"),
  גם אם התאריך שלו רחוק — משחקים זזים. **התאריך נקבע בשריקת הפתיחה**
  (`start` ראשון, `M.israelDate(op.at)`), לא בלוח. המשחק נושא `fixture`
  ({date, opponent} של השורה בלוח), `finishLive` שומר אותו, ו-`upcomingFixtures`
  מוריד את השורה לפיו — גם כשהתאריך שונה. **ביטול** (`clearLive` עם `discard`) לא שומר
  כלום, כולל שערים שתועדו, והשורה נשארת בלוח כי אף פעם לא הוצאה ממנו. משחק
  שהסתיים, נפתח מחדש לתיקון ואז בוטל — הגשר מוחק גם את השורה שהסיום הראשון
  כתב לעונה (לפי `liveId`); בלי זה הכובשים שלו נשארו והמשחק ירד מהלוח.
  "סגירת המסך החי" אחרי סיום (`clearLive` בלי `discard`) משאירה את התוצאה.

**כובשים בלוח התוצאות** (`scorersHtml`): טקסט כמו באתרי ספורט — נקודה, שם,
דקות — בשתי עמודות שוות, כל אחת ממורכזת מתחת לקבוצה שלה. בעל הריפו ניסה
תגיות ("פילים") לכל דקה ודחה אותן: הטקסט הוא הסטנדרט, הבעיה הייתה רק
היישור. שלנו מקובצים לפי שחקן (שלושער = שורה אחת עם שלוש דקות), עד 4 שורות
ואז "ועוד N". שערי היריבה (בלי שמות) הם רצף דקות אחד שנשבר בתוך העמודה, עד
16. רצף דקות הוא יחידה LTR (`dir="ltr"`, "5', 23', 37'") — בזרימת RTL הפסיקים
בין הדקות קפצו לצד הלא נכון. "ועוד N" הוא כפתור שגולל לציר הזמן ומדגיש את
השער הראשון שהלוח לא מציג (`data-goto` → `li[data-ev]`).

**לוח התוצאות בצמוד לקבוצות:** ב-`.sc-score` המספר שלנו מימין, ליד הסמל שלנו
(זרימה RTL), בניגוד לשורות ההיסטוריה (`.score`, LTR). בלוח עם שתי קבוצות,
הצמדת מספר לקבוצה חשובה יותר מסדר קריאה.

## המאמן: ניהול דקות

בעל הריפו הגדיר: **כלי מידע, לא מנהל.** אין הצעות חילוף, אין דירוג, ואין
סימון למי ששיחק הרבה — רק חוסר, בזהב. אל תוסיפו "המלצה" בלי לדבר עם בעל הריפו.

- **נתוני המאמן** בקובץ נפרד בדרייב, `coach.json`: `{ minDefault, matches:
  { [liveId]: { min, absent } } }`. לא ב-`live.json`: הורים קוראים אותו,
  המאמן לא שולט בלייב, ועריכה שלו לא צריכה להתנגש בתור של מי שמתעד. הדקות
  עצמן לא נשמרות — `src/minutes.js` מחשב מההרכב והחילופים.
- **רף:** ברירת מחדל 20 דקות במצטבר. רף שנקבע במשחק הופך לברירת המחדל,
  ומשחקים קודמים בלי רף משלהם מוקפאים בגשר על הקודם — אחרת שינוי היום היה
  משנה בדיעבד מי "היה מתחת לרף".
- **נוכחות:** מי שסומן חסר לא נספר (לא ברשימה, לא בהתראה, לא בעונה). מי
  שפתח או נכנס בחילוף נחשב נוכח, מה שלא יהיה כתוב.
- **התראה אחת** (`shortfall`): בהפסקה שלפני התקופה האחרונה בלבד, לשחקנים
  **בספסל** שנוכחים ומתחת לרף, עם הדקה האחרונה שבה אפשר להיכנס ועדיין להגיע.
  מי שעל המגרש יגיע אם יישאר — מסומן ברשימה, לא בהתראה. הודעה קופצת ורטט
  פעם אחת למשחק (`mg:alerted`, `checkMinutesAlert` ב-`app.js`), ו**כרטיס במסך
  הבית לכל אורך ההפסקה** (`homeAlertHtml`) — הודעה של 15 שניות נעלמת בזמן
  שהמאמן עסוק. "הבנתי" בלשונית מקפל את ההתראה לשורה (`mg:folded`) ומוריד את
  הכרטיס מהבית.
- **העיצוב נגזר ממוקאפ שבעל הריפו בחר** (השוואה מוקאפ/מימוש): מספר חולצה
  כטקסט אפור (`mn-num`) ולא עיגול זהב — הזהב ברשימות האלה שמור לחוסר; מקרא
  מעל הרשימה; "שיחק 30'–42'" לשחקן שירד (`ranges` ב-`liveRows`); נוכחות
  במתג "הגיע | חסר" עם קו חוצה לחסר; משבצות סיכום קטנות (`mn-kpis`); פס כחול
  בהיסטוריה למשחק שפתח בו; תפקיד במסך הגישה במתג "הורה | מאמן".
- **שחקן על המגרש חי** (`.mn-row.live`, רק כשהשעון רץ): הנקודה פועמת בלבן
  (לא זהב — זהב כאן הוא חוסר, ופעימה זהובה היא המשחק החי), הגרש של הדקות
  מהבהב (`mn-tick`), וקצה הפס זוהר. `prefers-reduced-motion` מכבה הכל.
- **עונה:** רק משחקי לייב (`liveId` + הרכב). ממוצע = על משחקים שהשחקן הגיע
  אליהם. בעל הריפו: "אין משחקים ישנים" — השדה הידני "דקות לפני הלייב" הוסר.
- **`cleanCoach`** בונה מחדש את נתוני המאמן שדה-שדה, ו-`int` מחזיר `null`
  ל-`null` (ולא 0 כמו `Number(null)`): ניקוי כפול הפך רף שלא נקבע ל-0 ואף אחד
  לא היה "מתחת לרף". יש על זה בדיקה.

## לוח משחקים

`fixtures` בנתוני העונה: `{date, time, opponent, home, round, venue}` — משחקים
שעוד לא נערכו. **"המשחק הבא" מחושב** (`buildSeason`): זה שהמנהל קבע ידנית
(עם התכנסות ותלבושת), אחרת המשחק הקרוב בלוח (`fixtureAsNext`). תאריך שיש לו
תוצאה ב-`matches` יורד מהלוח לבד — אין שדה "שוחק". בלי שעה: "שעה טרם נקבעה"
ובלי ספירה לאחור (`timeTbd`).

**משחק אימון** (בקשת בעל הריפו): אפשרות בשדה "מחזור" (select: "—", "משחק אימון",
1–40), נשמר כ-`friendly: true` ו-`round: null`, ומוצג ב-`roundText(round, friendly)`.
הוא ברשימות התוצאות וההיסטוריה, אבל **לא בשום נתון**: מאזן, בית/חוץ, רצפים,
פילוח הכושר (`s.form`) ושערי שחקנים נספרים מ-`league` ב-`buildSeason`. דקות
המאמן כן נספרות — זה זמן משחק. הדגל עובר מהלוח למשחק החי (`newLive`, `cleanLive`)
ו-`finishLive` כותב אותו בוליאני; בייבוא, "אימון"/"ידידות" בעמודת המחזור.

ייבוא (`src/fixtures.js`, מסך הניהול → "לוח משחקים"): לפי **שורת כותרות בלבד**
— תאריכים, מחזורים ושערים הם כולם מספרים קטנים, וניחוש לפי תוכן היה שם משחק
ביום הלא נכון. שני פורמטים: של ההתאחדות (קבוצת בית/חוץ, שערי בית/חוץ — "אנחנו"
= הקבוצה שבכל השורות) ושלנו (יריבה, בית/חוץ, שערים שלנו/היריבה). תאריך
dd/mm/yyyy או תא תאריך של אקסל. שורה עם תוצאה נכנסת ל-`matches`, אלא אם כבר
יש משחק באותו תאריך (משחק חי או ידני לא נדרס). הלוח מהקובץ מחליף את הקיים.

"מחיקת כל המשחקים": תוצאה מקובץ ותוצאה שהוקלדה נראות אותו דבר בנתונים, ולכן
המחיקה לפי קבוצות עם ספירה — לוח, תוצאות ידניות/מקובץ (מסומנים), משחקי לייב
(`liveId`, **לא** מסומנים — יש בהם שערים, חילופים והרכב שאי אפשר להקליד מחדש).

**המגרש הביתי** (`team.homeVenue` — ניהול → הגדרות → "המגרש הביתי"): ברירת
המחדל של כל משחק בית שאין לו מגרש משלו — בלוח, במשחק הבא וב-Waze. ממולא
ב-`buildSeason` (`withHomeVenue`) בכל טעינה ולא נכתב למשחקים: שינוי המגרש
הביתי מזיז את כולם. מגרש שהוזן למשחק גובר; משחק חוץ לא מקבל כלום.

**קישור Waze** (`wazeLink`): `https://www.waze.com/ul?q=<כתובת בלבד>`. בלי
`navigate=yes` — הוא מתחיל ניווט רק לקואורדינטות (`ll`), ועם `q` האפליקציה
נפתחה ריקה. בלי שם המגרש לפני הכתובת, ועם `www.` (הדומיין בלעדיו מפנה).
השדה "קישור Waze" במשחק הבא גובר על הכתובת, ומקבל גם **קואורדינטות**
(`31.956020,34.834553` — מה שגוגל מפות מציג למיקום ששותף בוואטסאפ; בעל הריפו
ביקש): `navLink` הופך אותן ל-`ul?ll=…&navigate=yes`. מחרוזת שאינה קישור מלא או
קואורדינטות נדחית בשמירה — `safeUrl` היה מקבל אותה כקישור יחסי שבור.

`docs/fixtures/schedule-demo.xlsx` (לוח פיקטיבי) ו-`schedule-template.xlsx`
(תבנית), עם גיליון הסבר. `tests/units.mjs` קורא את קובץ הדמו — שינוי בייבוא
ששובר אותו נכשל שם.

## תמונות לסרטונים (הדפוס מ-nines)

- **יוטיוב:** `i.ytimg.com/vi/<id>/hqdefault.jpg` ישירות מהדף — כתובת ציבורית
  קבועה, בלי גשר.
- **כל השאר:** בשמירה של המנהל (`preparePosters` ב-`src/posters.js`) הגשר מכין
  תמונה (`makePoster`, מנהל בלבד): קובץ דרייב → התמונה המוקטנת של דרייב, אחרת
  `og:image` מהדף דרך `UrlFetchApp`. היא נשמרת בתיקייה `posters` בדרייב, והסרטון
  מחזיק `poster` (מזהה הקובץ) ו-`posterFor` (הקישור שממנו נוצרה — קישור שהשתנה
  מקבל תמונה חדשה).
- **הורים** מקבלים אותה ב-`getPoster` ושומרים ב-IndexedDB (`mg-posters`) — פעם
  אחת לטלפון, וגם בלי רשת. **`getPoster` מחזיר רק קבצים שבתוך `posters/`**:
  `ref` הוא מזהה קובץ בדרייב, ובלי הבדיקה הורה היה קורא דרך הגשר כל קובץ של
  המנהל. `tests/bridge.mjs` מנסה לקרוא כך את `season.json`.
- `UrlFetchApp` דורש הרשאת "גישה לשירות חיצוני" — בפריסה הראשונה אחריו גוגל
  מבקש לאשר הרשאה חדשה.

## הגלריה של הקבוצה (תמונות וסרטונים מההורים)

בעל הריפו: לא בדרייב שלו, ו**לא תחושה שהאפליקציה של המנהל** — היא של
הקבוצה. לכן פרסום מיידי ובקרה בדיעבד, ולא אישור מראש.

- **הקבצים ב-Cloudinary**, מועלים ישירות מהטלפון (`src/gallery.js`, XHR בשביל
  פס התקדמות). הגשר רק חותם (`signUpload`): הוא בוחר את `public_id`
  (`mg/<אקראי>`) וחותם גם על `allowed_formats` — תמונה או סרטון, לפי מה שהתבקש.
  הסוד (`CLOUDINARY_SECRET`) ב-Script properties, יחד עם `CLOUDINARY_CLOUD`
  ו-`CLOUDINARY_KEY`. בלי שלושתם `getGallery` מחזיר `enabled: false` והגלריה
  לא מוצגת בכלל.
- **הרשימה ב-`gallery.json`** בדרייב, לא בעונה: הורים כותבים אליה, והיא לא
  צריכה להתנגש ב-`baseVersion` של המנהל. `addGalleryItem` מקבל רק `pid` שנחתם
  לאותו מכשיר (במטמון, `sig:<pid>`) — אחרת הורה היה רושם קובץ של אחר כשלו.
- **תמונה מוקטנת בטלפון** (canvas, 1600px, JPEG) — גם מהירות וגם הסרת EXIF
  (מיקום GPS). סרטון לא מוקטן: עד 60 שניות ו-100MB, נבדק בטלפון.
- **הסתרה בידי כל הורה** (`hideGalleryItem`, "הילד שלי בתמונה" / "לא מתאימה"):
  נעלם מיד לכולם חוץ מהמעלה. המעלה מוחק את שלו (`deleteGalleryItem`, גם
  מ-Cloudinary דרך `UrlFetchApp`). המנהל: `restoreGalleryItem`, מחיקה,
  `blockUploader` (חסימת העלאות בלי לבטל גישה), ומתג `setGallery`:
  `open` | `review` (ממתין לאישור) | `closed`, ומגבלה יומית לכל מכשיר (24
  שעות אחורה). המתג זז מיד ומראה "שומר…" עד שהגשר עונה (שנייה-שתיים; בלי
  זה נראה כאילו הלחיצה לא נקלטה — e2e מאט את הגשר ובודק).
- **מחיקה של כמה בבת אחת** ("בחירה" בעמוד של משחק, `sel`): הורה בוחר רק מהעלאות
  שלו, המנהל מכולן. `deleteGalleryItem` מקבל `ids` (עד 100), הכל-או-כלום —
  בחירה שיש בה פריט של אחר נדחית כולה. המחיקה ב-Cloudinary ב-`fetchAll`, במקביל.
- **הורה לא מקבל** מזהי מכשירים ולא מי הסתיר (`publicItem_`). אצל המנהל:
  ניהול → תוכן, עם מונה על הלשונית ונקודה על "ניהול" (`galleryWaiting`
  ב-`adminPing`).
- **בממשק ההורים אין מילה על מנהל** — לא "ממתין לאישור המנהל", לא "המנהל
  הסיר". כפתור "?" ליד הכותרת פותח הסבר (איך מעלים, הסתרה, לאן זה עולה);
  `tests/e2e.mjs` בודק שהמילה "מנהל" לא בו.
- **מקום אחד לסרטונים** (בעל הריפו: שני אזורי סרטונים בלבלו). העלאה אחת
  לתמונות וסרטונים, והתצוגה מפוצלת במתג "תמונות | סרטונים" (`shownTab`).
  לשונית הסרטונים מחזיקה גם את הסרטונים המקושרים של המנהל (`linkedVideosHtml`
  מ-`media.js`: תקצירים, משחקים מלאים) וגם את הקטעים של ההורים. כשהגלריה
  כבויה, המארח `[data-gallery]` נשאר עם הסרטונים המקושרים בלבד, כמו פעם.
  "קישורים שימושיים" נשאר אזור נפרד מתחת.
- **אלבומים, לא רשת אחת ארוכה** (בעל הריפו: "הדף לא יהיה ארוך כאורך הגלות";
  נבחר מול מוקאפ בגובה עמוד אמיתי). בסקירה: "העלאות אחרונות" — שורה אחת
  שנגללת הצידה (`STRIP`) — ו"לפי משחק", כרטיס אלבום לכל משחק, שישה
  (`ALBUMS_SHOWN`) ו"כל המשחקים · N". 12 משחקים ≈ 1,300px, וזה לא גדל עם העונה.
  לחיצה על אלבום פותחת את עמוד המשחק (`album`, בלי נתיב חדש — "הגלריה" חוזר,
  ושניהם לראש העמוד, `scrollTo(0)` — לא לתחילת הגלריה, שהשאירה את הדף גלול):
  כל התמונות, "בחירה" למחיקה (רק שם), ו"העלאה למשחק הזה" שבוחרת את המשחק מראש.
  **המתג "תמונות | סרטונים" נשאר גם בעמוד המשחק** (בעל הריפו), תמיד — גם במשחק
  בלי סרטונים, עם מצב ריק משלו; מתג שנעלם ומופיע מבלבל.
- **התשובה האחרונה של הגלריה נשמרת בטלפון** (`mg:gallery`) והמסך מצויר ממנה
  מיד. בלי זה הביקור הראשון בכל פתיחה הציג לשנייה את מסך "טרם הועלו סרטונים"
  שלפני הגלריה, ואז החליף — בעל הריפו ראה "גרסה קודמת". מכשיר שאין לו עותק
  מקבל "טוען…". הלשונית (תמונות/סרטונים) לא ננעלת לפי העותק: היא נקבעת מחדש
  בתשובה הטרייה, ומשם נשארת (`tabFixed`) — כדי שלא תקפוץ כשנמחקה התמונה האחרונה.
- **הגלריה לא מצוירת מחדש כשלא השתנה כלום** (`refresh({ quiet })` בטעינה):
  ציור מחדש מחליף כל תמונה ממוזערת (הבהוב) ואת שדה הקובץ — ב-e2e זה הפיל
  את פתיחת גיליון ההעלאה בכשליש מההרצות. אחרי פעולה (העלאה, מחיקה) מציירים תמיד.
  "חדש" על אלבום שעלה אליו משהו ב-3 הימים האחרונים.
- **לאיזה משחק** (`photoMatches` ב-`fixtures.js`): כל תוצאה, וגם משחק מהלוח
  שהיום שלו הגיע בלי שהוזנה לו תוצאה — מצלמים גם כשאף אחד לא הקליד תוצאה.
- `renderMedia` מצייר את המארח, `wireGallery` טוען ומצייר את הגלריה, עם צופה
  במסך מלא מתחת לגיליונות (z-index 55).

## סמל הקבוצה היריבה

בקשת בעל הריפו: במשחק הבא (ניהול → משחקים, `logoHtml`) המנהל מעלה תמונה של
היריבה, והיא מוצגת בכרטיס המשחק הבא ובלוח התוצאות בלייב, במקום שתי האותיות.

- **מעובדת בטלפון** (`uploadLogo` ב-`src/posters.js`, PNG בשביל שקיפות): שוליים
  שקופים נחתכים (`opaqueBounds` ב-`src/imaging.js`), הקטנה בחצאים באיכות גבוהה,
  חידוד עדין (`unsharp`, משוקלל לפי שקיפות — בלי הילה כהה סביב שלט), ועד 512px.
  PNG מפורט שעובר את `MAX_LOGO_BYTES` (200KB) בגשר יורד ברזולוציה עד 256. היא
  נשמרת בגשר (`putLogo`, מנהל בלבד) בתיקייה `posters` — הורים מקבלים אותה
  ב-`getPoster` ושומרים ב-IndexedDB, כמו תמונות הסרטונים. **לא בנתוני העונה**:
  תמונה בתוך `season.json` הייתה מנפחת אותו לתקרת 400KB.
- **רקע אחיד יורד לבד בהעלאה** (`keyBackground` ב-`src/imaging.js`): ירוק של
  גמיני, דף לבן, או משבצות אפור-לבן מצוירות. רק מה שנוגע בשוליים ובצבע השוליים
  (הצפה מהקצה) — לבן של אותיות בתוך טבעת נשאר. השוליים חייבים להיות צבע אחד
  כמעט כולם (או שני אפורים בהירים — משבצות), אחרת לא נוגעים: סמל שחתוך עד הקצה
  היה נחשב "צבע רקע שני" ונמחק. פיקסלי קצה מקבלים שקיפות חלקית, וחלק הרקע
  מוצא מהצבע שלהם (בלי שוליים ירוקים).
- **שיפור סמל בגמיני** (ניהול → הגדרות, "העתקת ההנחיה", `CREST_PROMPT`
  ב-`admin.js`, בקשת בעל הריפו): ההנחיה נבדקה מול גמיני בכמה סבבים, וכל שורה בה
  נולדה מכשל. בבקשה ל"השוואה" גמיני צייר השוואה עם כותרות לתוך התמונה; בבקשה
  לשקיפות צייר משבצות ב-JPEG; את הכחול הכהה-סגלגל הבהיר פעמיים (גוון 239°
  במקום 246°) ו"דגם" אותו לא נכון. לכן: תמונה אחת, סמל אחד, רקע `#00FF00`,
  וצבעים במילים ("כהה נשאר כהה"), לא בקודים. שינוי בהנחיה — רק אחרי ניסיון
  על סמל אמיתי.
- **לפי שם היריבה** (`opponentLogos: { [שם]: ref }`, `logoKey`/`opponentLogo`
  ב-`season.js`), לא לפי משחק: היא מגיעה ללייב בלי שדה ב-state (ובלי שורה
  ב-`cleanLive`), גם כשהלייב נפתח ממשחק אחר בלוח, וחוזרת בסיבוב השני.
  `buildSeason` בונה את המפה מחדש ומשאיר רק מזהה דרייב — הוא נכתב לתכונה ב-HTML.
- **לוחית אחת לשני הסמלים** (בקשת בעל הריפו): עיגול כהה (`--plaque`), טבעת דקה
  בהירה וצל עדין; הסמל בפנים עם שוליים (שלט של יריבה 12%, כדי שהפינות לא
  ייחתכו). ההילה הזהובה רק אצלנו — כרטיס מוביל אחד בזהב. `assets/crest.png`
  הוא תוצאת `CREST_PROMPT` בגמיני על הסמל מאתר המועדון (בעל הריפו): הרקע הירוק
  הוסר, נחתך לעיגול נקי, והצבעים יושרו להורדה (צהוב `#FCE306`, כחול `#1B1555`).
  512px, PNG עם פלטה. לא לחדד ולא לצבוע מחדש.
- האותיות נשארות מתחת עד שהתמונה הגיעה (`.opp-logo:not([src])`), ותמונה שכבר
  נטענה בסשן מצוירת מיד (`cachedPosterUrl`) — הלייב מצייר מחדש כל כמה שניות.
- `getPoster` נשלח עם קוד המנהל כשיש: הטלפון של המנהל לא חייב להיות מכשיר מאושר.

## עמדות וייבוא

`src/positions.js` — מזהה קצר בנתונים (`GK`, `LB`…), תווית בעברית בתצוגה,
ומיקום על המגרש. `matchPosition` מזהה טקסט חופשי ("מגן שמאלי", "LB"). לשחקן
`pos` ו-`pos2`; שחקן ישן עם `position` טקסטואלי מתורגם בקריאה.

**סדר המחליפים** (`subGroups`, הגדרה של בעל הריפו): אותה עמדה כעמדה ראשית,
אחר כך כעמדה שנייה; העמדות הסמוכות (`NEAR`: לקשר אחורי ולקשר קדמי — קשר
מרכזי); ואז קווים לפי הקו של מי שיוצא (`LINE_ORDER`): הגנה → הגנה, קישור,
התקפה; התקפה → התקפה, קישור, הגנה; קישור → קישור, התקפה, הגנה. קשר קדמי הוא
קישור. שוער אינו הגנה. בתוך קו: עמדה ראשית ואז שנייה, ושחקן נכנס לקו הראשון
שמתאים לו בכל אחת מהן. מהספסל למגרש — אותו סדר לפי עמדות הנכנס (`also`).

`src/importer.js` — xlsx בלי ספרייה (zip + `DecompressionStream('deflate-raw')`,
XML בביטויים רגולריים כדי שירוץ גם ב-Node), CSV ב-UTF-8 או Windows-1255
(אקסל עברי), והדבקת תאים. התאמה לפי שם ואז מספר; מזהים קיימים נשמרים.
שחקנים בלי `id` מקבלים `'n:' + name` — אותו כלל ב-`season.js` ובעורך.

## כלל הברזל: לא לשמור מה שאפשר לחשב

נתוני העונה מחזיקים **עובדות בלבד** — משחקים (כולל אירועי משחק חי), שחקנים,
סרטונים, קישורים. שערים ובישולים של שחקן = בסיס ידני ("לפני הלייב")
ועוד ספירה מאירועי המשחקים, מחדש בכל טעינה — מחיקת שער שגוי מתקנת גם את הטבלה.
כל סיכום (נקודות, אחוזים, ממוצעים, רצפים, פילוח בית/חוץ) מחושב ב-`src/season.js`
בזמן ריצה, ולמסך הניהול אין שדה לאף אחד מהם. גם קישור ה-Waze נגזר מהכתובת,
ותווית העונה ("2026/27") מחושבת מהתאריך כשהמנהל השאיר אותה ריקה (`seasonLabel`).
placeholder בשדה ריק נראה כמו ערך שמור — זה מה שהסתיר את העונה אצל בעל הריפו.

זה לא סגנון — המוקאפים שמהם נבנתה האפליקציה כבר סתרו את עצמם: טבלת בית/חוץ
הכריזה "3נ · 0ת · 0ה" בזמן שרשימת המשחקים באותו מסך הכילה הפסד ביתי.

## מסך הניהול מונע-טבלה

`src/views/admin.js`: הטבלה `LISTS` מגדירה לכל רשימה (משחקים, שחקנים, סרטונים,
קישורים, תובנות) את השדות, התבנית להוספה, התווית והוולידציה. **הוספת שדה =
שורה בטבלה**, לא קוד ממשק חדש. כל שדה שנוסף חייב להיות מטופל גם בתצוגה, או
שהוא נשמר ולא נראה בשום מקום.

**לשוניות לפי תדירות שימוש** (`TABS`, בקשת בעל הריפו: פחות גלילה, פחות אי-סדר):
משחקים (המשחק הבא, תוצאות, לוח) · שחקנים · תוכן (סרטונים, קישורים, תמונת מצב) ·
הגדרות (קבוצה, מבנה משחק) · גישה. לכל רשימה ב-`LISTS` יש `tab`. כל רשימה היא כרטיס
אחד של שורות (`sum`: כותרת, שורת משנה, תוצאה), רשימה עם `limit` מציגה את השורות
הראשונות ו"הצגת כל ה-N". כלי ייבוא ומחיקה מקופלים מאחורי "ייבוא" בכותרת האזור —
הם פעולות של פעם בעונה. **שורה חדשה נפתחת בראש הרשימה** (בעל הריפו: בסוף היא נפתחה
מחוץ לעין) והמסך גולל לאזור שלה. **שורה אחת פתוחה בכל רשימה** (פתיחת שורה סוגרת את
האחרות), **ושמירה מוצלחת סוגרת את כולן** — שמירה כושלת משאירה אותן פתוחות.
**לכל רשימה עם סדר טבעי יש `order` ב-`LISTS`** — לוח מהקרוב, תוצאות מהאחרונה,
סגל לפי מספר חולצה (`byNumber` מ-`format.js`, אותו סדר כמו בלייב), סרטונים כמו
שההורים רואים (`videoOrder` מ-`media.js`). `sortLists` ממיין בטעינה, אחרי ייבוא
ובשמירה — לא תוך כדי הקלדה, כי שורה שקופצת מתחת לאצבע גרועה משורה לא במקומה.
קישורים ותובנות בלי `order`: הסדר שבו הוזנו הוא הסדר שההורים רואים. שורה חדשה
נכנסת **לסוף הנתונים** ומוצגת **ראשונה** עד השמירה (`__new`, האחרונה למעלה) —
ההצגה לא משנה את הסדר ששומרים.
שדה עם רמז לידו נמתח לגובה השורה — `.field` עם `align-content: start`, אחרת
תיבת התאריך גדלה ליד תיבת השעה. פס השמירה משותף לכל הלשוניות, ושגיאת שמירה פותחת את
הלשונית של השדה הראשון שנכשל (`errs.tab`). לשונית חדשה = שורה ב-`TABS` וענף
ב-`seasonHtml`.

כל רשימה בנתונים רשאית להיות ריקה או חסרה; `buildSeason` משלים ברירות מחדל.
כל תצוגה חדשה חייבת מצב ריק משלה. שדות אופציונליים (שם מקוצר, קישור Waze)
נופלים לשדה המלא ולא מגיעים לדף כ-`undefined` — זה בדיוק מה שהבדיקות תפסו
כשהנתונים הפסיקו להגיע מקובץ דמו.

## שני מוקשים שכבר נדרכו כאן

**תאריכים.** תאריך משחק הוא תאריך לוח, לא רגע בזמן. `shortDate` מפרק את
המחרוזת `YYYY-MM-DD` ישירות ולא עובר דרך `Date`, ומחזיר `dd.mm.yy` — עם שנה,
בקשת בעל הריפו (עונה חוצה שנה). בעמודת התאריך הצרה של שורת משחק (`whenHtml`
ב-`components.js`) השנה בשורה שנייה, קטנה — "03.10.26" בשורה אחת ב-Rubik גלש
לפיל בית/חוץ. e2e מודד שאין חפיפה. במקומות צרים אחרים (עיגול בבחירת משחק
ללייב, כותרות עמודות במפת הדקות) — יום וחודש בלבד. מועד פתיחה נשמר כרגע עם
היסט ישראלי (`+03:00` קיץ, `+02:00` חורף); `israelIso` מוצא את ההיסט דרך
`Intl` ולא דרך תאריכי שעון קיץ קבועים, כי אלה נקבעים בהחלטת ממשלה.

**תוצאות.** השערים שלנו תמיד ראשונים ובזהב. הציון נבנה משני מספרים נפרדים,
ול-`.score` ול-`.form-pill b` יש `direction: ltr; unicode-bidi: isolate` מפורש.

## סמל הקבוצה ≠ אייקון האפליקציה

`assets/crest.png` הוא סמל המועדון, ומוצג בכל מקום שהממשק אומר "אנחנו".
`icons/` הם אייקון האפליקציה — המוצר, לא הקבוצה. בעל הריפו ביקש במפורש
להפריד ביניהם. הנתיב יכול להיקבע ב-`team.crest`; ברירת המחדל ב-`src/config.js`.

## צבע ושפה עיצובית

זהב (`--gold`) הוא המותג. ירוק ואדום שמורים **אך ורק** לתוצאות משחקים —
ברגע ששני התפקידים מתערבבים, פיל "ניצחון" מפסיק לבלוט משאר הממשק. לכן גם
שגיאות, מחיקה ומונה בקשות אינם אדומים: שגיאה בזהב, מחיקה ניטרלית, מונה בזהב.

כחול (`--blue`) הוא צבע הסדרה השנייה — בישולים, משחקי חוץ — לא מצב.

השפה (אושרה מול מוקאפ של בעל הריפו):

- **רקע נייבי עם זוהר מקובע למסך** (`body::before`, `position: fixed`): כחול
  למעלה, ירקרק באמצע משמאל, כחול למטה. הכרטיסים גוללים מעליו, ולכן אותו מרווח
  נראה כחול למעלה וירוק באמצע — זה האפקט שבעל הריפו ביקש. לא
  `background-attachment: fixed` (אייפון מתעלם), ורקע ה-`body` שקוף כדי לא
  לכסות את השכבה. הירוק הזה עמום ואמביינטי בלבד — אף רכיב ממשק לא ירוק חוץ
  מתוצאה.
- **כרטיסים שקופים-למחצה** מעל הזוהר, בלי `backdrop-filter` — הרקע כבר רך,
  וטשטוש על כל כרטיס עולה בחלקות הגלילה בטלפון. טשטוש רק על אלמנטים צפים
  (ניווט, פס שמירה, טוסטים).
- **רק כרטיס מוביל אחד בכל מסך זוהר בזהב**: המשחק הבא, לוח התוצאות בלייב,
  כרטיס השער. והכפתור הראשי. אם הכל זוהר, שום דבר לא בולט.
- **כותרת אזור = אייקון זהב + כותרת** (`sectionHead(title, aside, glyph)`).
- **ניווט צף בתחתית**. `body.with-nav` שומר לו מקום ומרים מעליו את פס השמירה,
  הטוסטים ופס העדכון. אלמנט `fixed` חדש בתחתית המסך חייב לעשות אותו דבר.
- ספרות ב-Rubik (`.num`), טקסט ב-Assistant.
- **שם שחקן:** שם מלא בכל מקום שיש לו מקום. היכן שאין (מגרש, ספסל) —
  `shortName` מ-`format.js`: שם פרטי ואות ראשונה של שם המשפחה עם נקודה
  ("אורי כ."), לא שם פרטי בלבד — שני שחקנים עם אותו שם פרטי נבדלים.

**גריד עם שדות: `minmax(0, 1fr)`, לא `1fr`.** באייפון לשדה תאריך/שעה ול-select
יש רוחב מינימלי משלהם; עם `1fr` הם גלשו לתא השכן והפיקרים נגעו זה בזה.

**מתגים** (`seg`, וגם "הגיע | חסר", "הורה | מאמן", ובחירת מבנה המשחק `chip-btn`): שקע כהה, והאפשרות שנבחרה
בזהב שקוף עם מסגרת זהב — לא זהב מלא וזוהר. בעל הריפו בחר את זה מהמוקאפ:
מתג הוא רהיט, וזהב מלא התחרה בכרטיס המוביל של המסך.

**גיליון (sheet) שייך למסך שבו נפתח:** כל שינוי נתיב סוגר גיליונות פתוחים
(`sheet.js`). כפתור "חזרה" באנדרואיד שינה את הנתיב מתחת לגיליון, והגיליון נשאר
מעל המסך הבא.

מסך חדש נבנה מהרכיבים הקיימים (`card`, `sec-head`, `btn`, `field`, `seg`,
`chip`) ומקבל את השפה בלי CSS משלו. צבע או צל בקוד קשיח במקום משתנה הוא איך
מסך יוצא מהשפה.

## גרסאות ועדכונים

בעל הריפו מתקין את האפליקציה כקיצור דרך במסך הבית, ומצפה שכל שינוי יגיע
אליה בלי התקנה מחדש. המספר מוצג בתחתית כל מסך ("גרסה 1.0.0"), כדי שתמיד
יהיה ברור איזו גרסה רצה.

**כל דחיפה ל-`main` שמשנה את האפליקציה — `node tools/bump.mjs`** (`patch`
לתיקון, `minor` ליכולת חדשה). הסקריפט כותב את הגרסה לשלושה מקומות:
`version.json` (מה שהשרת מכריז), `CACHE_VERSION` ב-`sw.js`, ו-`_BUNDLE_VERSION`
ב-`index.html` (מה שרץ). לעולם לא לערוך אותם ידנית.

איך עדכון מגיע (`src/updater.js`, מ-`family-vault`):

- **ה-service worker הוא network-first**, בכוונה ובניגוד ל-`family-vault`. כשיש
  רשת, כל פתיחה מקבלת את הקוד העדכני — גם אם שכחו לקדם גרסה. המטמון הוא רק
  גיבוי לאופליין. אין להפוך אותו ל-cache-first בלי לדבר על זה: זה בדיוק מה
  שהיה גורם לעדכונים "להיתקע" בטלפונים.
- **הקידום הוא מה שגורם לאפליקציה פתוחה לשים לב.** אפליקציה ממסך הבית
  באייפון לא נטענת מחדש — היא חוזרת מהרקע. בכל חזרה לחזית נבדק
  `version.json`; אם הוא שונה ממה שרץ, ה-SW נרשם מחדש והדף נטען מחדש, פעם
  אחת לכל צמד גרסאות (בולם לולאות).
- **עריכה של מנהל שלא נשמרה, גיליון פתוח (שער באמצע הזנה) או פעולות לייב
  שעוד לא נשלחו חוסמים את הטעינה האוטומטית** — במקומה מופיע פס
  "יש גרסה חדשה". `tests/pwa.mjs` בודק את זה בהתנהגות, לא בחיפוש טקסט, ונבדק
  שהוא נכשל כשהבולם נשבר.
- **כל קובץ חדש תחת `src/` נכנס ל-`CORE` ב-`sw.js`**, אחרת האפליקציה נשברת
  בפתיחה הראשונה בלי רשת. הבדיקה נכשלת אם שכחו.
- ה-SW לא נוגע בבקשות לדומיין אחר (הגשר, גוגל פונטס, Waze) ולא ב-`version.json`.

## התקנה והזמנה

- **הסבר התקנה** (`src/install.js`, שתי המערכות מקופלות כברירת מחדל — בקשת בעל הריפו) במסך בקשת הגישה ובמסך ההמתנה, נעלם כשהאפליקציה
  רצה ממסך הבית. **באייפון: מתקינים לפני שמבקשים גישה.** אפליקציה ממסך הבית
  לא חולקת אחסון עם Safari — מפתח מכשיר אחר, כלומר מכשיר חדש לגשר — ובקשה
  שנשלחה מ-Safari לא עוברת לאייקון. באנדרואיד האחסון משותף, ו-Chrome מציע
  התקנה בלחיצה (`beforeinstallprompt` → כפתור "התקנה").
- **בטלפון, בקשת גישה נשלחת רק מהאפליקציה המותקנת** (`requestGated`), באייפון
  ובאנדרואיד — בקשת בעל הריפו. בדפדפן בטלפון אין טופס, רק הוראות וקישור
  "אי אפשר להתקין? להמשיך בלי התקנה" (`state.skipInstall`, לביקור הנוכחי בלבד;
  באייפון עם אזהרה). מחשב ודפדפן אחר מקבלים את הטופס ישר — אין שם מסך בית.
  זה לא נאכף בגשר: הגשר לא יכול לדעת אם הבקשה הגיעה מאפליקציה מותקנת, וזו
  שאלת נוחות, לא אבטחה. בהוראות: קישור שנפתח בתוך וואטסאפ → "פתיחה ב-Safari/Chrome".
- **`[hidden] { display: none !important }`** — בלי זה `.btn` (flex) דורס את
  `hidden` והכפתור מופיע גם כשאין מה להתקין.
- **מסך ההמתנה בודק לבד** בחזרה לאפליקציה וכל 20 שניות — האישור מגיע בזמן
  שההורה בוואטסאפ. בדיקה שעדיין ממתינה לא מציירת כלום.
- **בקשות ממתינות** — נקודה זהובה קבועה (לא מהבהבת; הבהוב = משחק חי) על
  "ניהול" בסרגל (`.nav-dot`). נשאלת מ-`adminPing` בכל טעינת עונה, בחזרה
  לאפליקציה ופעם בדקה, ומתעדכנת כשמסך הגישה נטען (`onPending`).
- **הזמנה ומדריכים** (מסך הניהול → גישה): מתג "מקוצר | מדריך הורה | מדריך מאמן"
  (`inviteKind`, בקשת בעל הריפו) קובע מה ההעתקה וה-`wa.me` שולחים — הודעה עם
  כתובת האפליקציה, או קישור ל-`docs/parent.html` / `docs/coach.html`. אין בקישור
  שום פרמטר — קישור לא מעניק גישה; הגישה היא אישור המנהל.

## פריסה

GitHub Pages מענף `main`, שורש הריפו, בלי workflow. push ל-`main` הוא
הפריסה. הכתובת: <https://lironcon2015-prog.github.io/MGivatayim/>.

האתר מוגש תחת **תת-נתיב**, ולכן כל הנתיבים בקוד יחסיים, ונכסים נפתרים מול
`import.meta.url` ולא מול המסמך. אין `CNAME` בריפו בכוונה.

## בדיקות

```bash
npm i playwright            # פעם אחת, בשורש. node_modules מוחרג ב-.gitignore
node tests/units.mjs        # ייבוא (כולל xlsx אמיתי), ומודל המשחק החי דרך units-live.mjs
node tests/bridge.mjs       # כללי ההרשאה של bridge.gs — מריץ את הקובץ האמיתי
node tests/e2e.mjs          # הורה + מנהל בדפדפן, מול הגשר האמיתי על שירותי גוגל מדומים
node tests/pwa.mjs          # גרסאות מסונכרנות, precache שלם, ועדכון שמגיע לדף פתוח
```

בסשן מרוחק `.claude/hooks/session-start.sh` מתקין את playwright אם חסר. אותו
הוק מפעיל בכל סשן את הסקיל `token-efficient-workflow` (`.claude/skills/`).
`CLAUDE.md` גובר עליו.

הבדיקות מריצות את ה-Chromium שבתמונה (`/opt/pw-browsers`) — לא להריץ
`playwright install`. לצילומי מסך של האפליקציה: שרת סטטי על שורש הריפו,
`localStorage['mg:bridge']` לגשר מדומה (`page.route`), וחלון בגודל 390×844.

**בדיקה חדשה מוכיחה את עצמה:** אחרי שהיא עוברת, מסירים את התיקון (`git stash`
על הקובץ) ורואים שהיא נכשלת. בדיקה שעוברת גם בלי התיקון לא בודקת כלום —
כך נתפסו כאן כבר שתי בדיקות ריקות.

`tests/mock-bridge.mjs` מריץ את `tools/bridge.gs` עצמו ב-Node על גרסאות
בזיכרון של DriveApp, CacheService ושות'. אין "גשר מזויף" שמחקה את המקורי —
הבדיקות עוברות רק אם הקובץ שנפרס עובד.

## מוקאפים

- מוקאפ = עמוד HTML רגיל שמפורסם בכלי Artifact (file_path לקובץ HTML), והקישור נמסר בצ'אט.
- אסור להשתמש ב-Artifact type של Design/קנבס (quickstart עם intent "design", או type_url): הוא נפתח כקנבס בזום זעיר ו-Read-only, ולא נוח בנייד.
- העמוד נגלל כרגיל וקריא בטלפון בלי זום. המסכים זה לצד זה ויורדים לשורה חדשה במסך צר. כל מסך ברוחב טלפון (~300px), עם כותרת קצרה מעליו והסבר קצר מתחתיו.
