# מכבי גבעתיים — העונה

אתר סטטי שמרכז עונת כדורגל אחת של קבוצת נוער: המשחק הבא, תוצאות, נתוני
שחקנים, סרטונים וקישורים. קהל היעד הוא השחקנים וההורים, לא המאמן — המטרה
היא להחליף חיפוש בוואטסאפ, לא לנהל את הקבוצה.

עברית, RTL, mobile-first, כהה בלבד. מודולי ES טבעיים, בלי תלויות ובלי build.

## היסטוריה

הריפו הזה החזיק פעם אפליקציית תיק השקעות. היא עברה ל-`lironcon2015-prog/homebudget`
תחת `invest/` ולא חוזרת לכאן. דף העברת הנתונים שהחליף אותה נמחק אחרי שבעל
הריפו אישר שאין יותר דפדפן שמחזיק נתונים ישנים. הקוד שלו שמור ב-`15fb22a:index.html`.

## כלל הברזל: לא לשמור מה שאפשר לחשב

`data/season.json` מחזיק **עובדות בלבד** — משחקים, שחקנים, סרטונים, קישורים.
כל סיכום (נקודות, אחוזים, ממוצעים, רצפים, פילוח בית/חוץ) מחושב ב-`src/season.js`
בזמן ריצה. אין להוסיף שדות סיכום לקובץ הנתונים.

זה לא סגנון — המוקאפים שמהם נבנתה האפליקציה כבר סתרו את עצמם: טבלת בית/חוץ
הכריזה "3נ · 0ת · 0ה" בזמן שרשימת המשחקים באותו מסך הכילה הפסד ביתי. חישוב
מרשימת המשחקים נותן 3נ · 0ת · 1ה, והסכום הכולל נשאר 16 נקודות.

## שני מוקשים שכבר נדרכו כאן

**תאריכים.** תאריך משחק הוא תאריך לוח, לא רגע בזמן. `shortDate` מפרק את
המחרוזת `YYYY-MM-DD` ישירות ולא עובר דרך `Date` — להעביר אותו דרך אובייקט
תאריך זה בדיוק איך שיום וחודש מתהפכים אזור-זמן אחד מהבית. לריפו הזה כבר היה
קומיט בשם "Fix reversed dates in cards".

**תוצאות.** השערים שלנו תמיד ראשונים ובזהב, השערים של היריב שניים. הציון
נבנה משני מספרים נפרדים ולא ממחרוזת `"2:1"` בנתונים, ול-`.score` ול-`.form-pill b`
יש `direction: ltr; unicode-bidi: isolate` מפורש — בלעדיו הכיוון תלוי בכך
שאלגוריתם ה-bidi יחליט להתייחס ל-`2:1` כרצף מספרי אחד, וזה לא דבר שכדאי
לסמוך עליו כשהמחיר הוא תוצאה הפוכה.

## צבע

זהב (`--gold`) הוא המותג. ירוק ואדום שמורים **אך ורק** לתוצאות משחקים.
אין להשתמש בזהב לסימון ניצחון ואין להשתמש בירוק לכפתור ראשי — ברגע ששני
התפקידים מתערבבים, פיל "ניצחון" מפסיק לבלוט משאר הממשק. זו הייתה החולשה
של כיוון העיצוב המקורי ב-Stitch והסיבה שנבחר הזהב.

## פתוח

- **`CNAME` עדיין מצביע ל-`invest.lironcon.com`** — שם שגוי לאפליקציה הזו.
  שינוי דורש רשומת DNS אצל הרשם (רק בעל הדומיין) ועדכון הקובץ.
- הנתונים ב-`data/season.json` הם מציין מקום שנגזר מהמוקאפים. שמות
  השחקנים, התוצאות והיריבים אינם אמיתיים.
- `url` ריק בסרטונים ובקישורים — צריך למלא.

## בדיקה

`python3 -m http.server` ואז פתיחה בדפדפן. לעדכון שוטף של הנתונים: `README.md`.
